[project]
name = "adobe-analytics-rs-sync"
version = "1.0.0"
description = "Synchronize report suite configurations across Adobe Analytics environments"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "aanalytics2>=0.4.4.post1",
    "python-dotenv>=1.2.1",
]
